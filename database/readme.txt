ソースファイル gui.py
データベース detail.sqlite3 , charge.sqlite3

python環境が必須（標準モジュールのみなので追加ライブラリは不要）

実行はgui.pyとdetail.sqlite3とcharge.sqlite3が入ったフォルダでコマンドライン(terminal)を起動
python gui.pyを入力すると、GUI画面が出力される.

以下、GUIに従ってデータ追加、変更、削除を行う.

